Quantum Annealing README

how to build and run:
managed by "cargo"
in the same directory as the "Cargo.toml" file you can use

cargo build (compiles default debug version, --release for optimized version) 
cargo run (run main.rs in src/)
cargo add (add new package)
